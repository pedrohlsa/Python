# LOOP em sets (única forma de acessar itens)

thisset = {"apple", "banana", "cherry"}

# For loop direto
for x in thisset:
    print(x)

# Sets são desordenados, então a cada execução pode vir em ordem diferente
